import 'package:flutter/material.dart';

/// Design System Constants
/// Phase 1: Color and Geometry Standards

// Background Colors
const kBackgroundColor = Color(0xFF000000);
const kElevatedGray = Color(0xFF1F1F1F);

// Glow Colors
const kGreenGlow = Color(0xFF10B981);
const kRedGlow = Color(0xFFEF4444);

// Geometry
const kSquircleRadius = 16.0;
